#include "mainwindow.h"
#include <QApplication>
//#include "dialog.h"

int main(int argc, char *argv[])
{
#if 0
    QApplication a(argc, argv);
    MainWindow w;

    w.show();

    return a.exec();
#endif
}
